# Claude Code session artifacts
.claude/

# OS junk
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.iml

# Secrets / local env overrides
.env
.env.*
!.env.example
style/credentials

# Python (backend, once it exists)
__pycache__/
*.pyc
.venv/
venv/

# Caddy runtime state (if run locally from repo root)
infra/caddy/caddy
infra/caddy_data/
infra/caddy_config/

# Stray build output at repo root (Flutter build artifacts live under
# frontend/build/, ignored via frontend/.gitignore already)
/build/

# Архивы, скачанные с платформы хакатона: doc-1788600416.zip содержит
# private_test_ground_truth.csv. Пока идёт автопроверка, эти ответы не
# используются ни для обучения, ни для отбора моделей — и не должны
# попасть в репозиторий даже случайно.
ml/doc-*.zip

# Кэш калибровочных таблиц (scripts/eval_reblend.py, eval_multimodel.py) —
# производные pickle-файлы, пересчитываются за минуты, незачем коммитить.
ml/reports/calibration_cache/
