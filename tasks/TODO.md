# TODO — сводный список задач (2026-09-05)

Единая точка входа по всем открытым задачам проекта. Полный контекст и
детали — в [`frontend.md`](frontend.md), [`backend.md`](backend.md),
[`ml.md`](ml.md); здесь — короткий чек-лист без дублирования, плюс
свежие пункты, появившиеся после сегодняшнего мерджа веток.

## 🔴 Фоллоу-ап сегодняшнего мерджа `origin/main` → `master`

Сегодня в `master` (там, где сделан новый лендинг "Главная") влит
`origin/main` — параллельный командный редизайн (dashboard-сайдбар,
аналитика/уведомления/отчёты/настройки, шаринг карт, новый логотип).
Конфликты разрешены `git merge -X ours`: там, где правки пересекались
построчно, оставлен вариант `master` (в первую очередь — роутинг: `/`
это `HomeScreen`, `/map` — карта); всё остальное командное подтянулось
как есть.

- [ ] **Запушить мердж в `origin/master`** — коммиты пока только
      локально (`git log` показывает `master` впереди `origin/master`).
- [ ] **Смоук-тест смерженного фронта в браузере** — `flutter analyze`
      чистый, `flutter build web` проходит, но само поведение (сайдбар,
      аналитика/уведомления/отчёты/настройки, шаринг карт, тёмная тема)
      после автоматического слияния двух независимо написанных UI
      визуально не проверялось. Риск тихих регрессий выше обычного.
- [ ] **Решить судьбу `frontend/lib/screens/landing_screen.dart`** —
      командная версия лендинга, пришла мерджем, нигде не подключена в
      роутинге (используется наш `home_screen.dart`). Либо удалить как
      мёртвый код, либо забрать из него то ценное, что не попало в
      `home_screen.dart`, и удалить файл.
- [ ] **Перечитать `map_screen.dart` и `account_screen.dart` целиком**
      — обе стороны их правили (мы точечно, команда — существенно
      переписала под dashboard-сайдбар), финальная версия — смесь.
      `flutter analyze` прошёл, но это не гарантия корректного UX.
- [ ] **Проверить, что пункт «заглушки в сайдбаре» из `frontend.md`
      всё ещё актуален** — после мерджа `/analytics`, `/notifications`,
      `/reports`, `/settings` подключены в `app.dart` по-настоящему (не
      просто снэкбар «не готово»); нужно перепроверить, реальный ли там
      функционал или заглушки остались.

## Не закоммичено с начала сессии (не наши правки — не трогали)

- [ ] Разобраться с незакоммиченными правками в `backend/ml/`
      (`.env.example`, `.gitignore`, `README.md`, `requirements.txt`,
      `webapp/gee_utils.py`, `webapp/main.py`) и новым файлом
      `webapp/copernicus_utils.py` — неясно, доведены ли они до конца и
      готовы ли к коммиту. **Кому:** автор этих правок.
- [ ] `backend/ml/secrets/gee-service-account.json` лежит вне git
      (untracked) — убедиться, что путь действительно в `.gitignore` и
      никогда случайно не попадёт в коммит/пуш.

## Frontend (подробности — [`frontend.md`](frontend.md))

- [ ] Разделы сайдбара "Аналитика"/"Уведомления"/"Отчёты"/"Настройки" —
      решить всей командой, нужны ли они для сдачи (в ТЗ не требуются).
- [ ] Мобильная адаптация dashboard-сайдбара по референс-макету.
- [ ] Прогнать поиск по "чужому" региону end-to-end на реальном
      бэкенде (не только на моке) в разных географических областях.
- [ ] Актуализировать `frontend/README.md`.

## Backend (подробности — [`backend.md`](backend.md))

- [ ] Разместить GEE credentials на проде и задать `EARTHENGINE_PROJECT`
      (код готов, не хватает только деплой-шага).
- [ ] `backend/inference/run_inference.py` дублирует `ml_bridge` с
      худшим качеством (интерполяция вместо обученной модели) —
      унифицировать на одну и ту же модель для сдачи и веб-сервиса.
- [ ] Дождаться/добиться активации Brevo SMTP-аккаунта (подтверждение
      писем сейчас не отправляется реально).

## ML (подробности — [`ml.md`](ml.md))

- [ ] Синхронизировать две версии `ml/`: актуальная в `../ml/` (до v10)
      и более ранний снапшот в `../backend/ml/`, к которому реально
      подключён `ml_bridge.py` — свериться, какая версия нужна
      веб-сервису, если понадобится обновить модель.
- [ ] `backend/inference/run_inference.py` не на реальной модели
      (дублирует backend-пункт выше).
